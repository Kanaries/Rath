import { memo } from "react";
import { QueryEditorProps } from ".";


const SQLEditor = memo<QueryEditorProps>(function QueryEditor ({  }) {
    return (
        <></>
    );
});


export default SQLEditor;
