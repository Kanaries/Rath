import styled from 'styled-components';

export const MainCardContainer = styled.div`
    .seg-header{
        font-size: 2em;
        font-weight: 500;
    }
    .seg-desc{
        color: var(--foreground);
        margin-bottom: 1em;
    }
`